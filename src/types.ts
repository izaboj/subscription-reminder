export interface ListItems {
  icon: string;
  link: string;
  title: string;
}
export interface Payload {
  email: string;
  password: string;
}
export interface User {
  uid: string;
  email: string;
  accessToken: string;
}
export {};
